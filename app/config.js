const path = require("path")

const folder = {
    "temp": path.join(__dirname, "items", "temp"),
    "thumbnail": path.join(__dirname, "items", "thumbnail"),
    "file": path.join(__dirname, "items", "file")
}

module.exports = {folder}
